import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.basketballshots.app',
  appName: 'basketballShots',
  webDir: 'dist/basketball-game/browser',
  
};

export default config;
